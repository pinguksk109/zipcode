<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- @formatter:off -->
<mapper namespace="jp.co.ot.zipcode.infrastructure.repository.ZipcodeDbRepository">

	<insert id="saveZipcode">
		INSERT INTO zipcode (
			id,
			zipcode,
			address,
			kana
		) VALUES (
			#{dto.id},
			#{dto.zipcode},
			#{dto.address},
			#{dto.kana}
		);
	</insert>
	
	<select id="find" resultType="jp.co.ot.">
		SELECT 
			id,
			zipcode,
			address,
			kana
		FROM 
			training.zipcode;
	</select>



</mapper>